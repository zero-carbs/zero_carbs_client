import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.zerocarbs.app",
  appName: "zero_carbs",
  webDir: "dist",
  // server: {
  //   androidScheme: 'https',
  //   hostname: "zerocarbs.app",
  //   cleartext: true,
  // },
};

export default config;
